# Import this file in Vercel → Project → Settings → Environment Variables → Import .env
# Apply to: Production, Preview, and Development
# Root Directory must be: frontend

NEXT_PUBLIC_API_URL=/api/v1
BACKEND_URL=https://bankms-fullstack-2.onrender.com
NEXT_PUBLIC_APP_NAME=CoreBank MS

# Optional — next.config.ts auto-uses VERCEL_URL if omitted. Set after first deploy:
# NEXT_PUBLIC_APP_URL=https://your-project.vercel.app
